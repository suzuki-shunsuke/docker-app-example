#!/bin/bash

set -e

cd `dirname $0`/..

if [ $# -ne 1 ]; then
  echo "Usage: build <TAG>"
  exit 1
fi

TAG=$1

source ../bin/_docker-common
source ../bin/_util

erun docker build -f Dockerfile -t $IMAGE_PATH:dev-$TAG ..
